- type: worker
  name: ai-people-reader-v2-report-worker
  runtime: python
  region: singapore
  plan: starter
  buildCommand: |
    set -e
    apt-get update
    DEBIAN_FRONTEND=noninteractive apt-get install -y chromium libreoffice libreoffice-writer fonts-noto-core fonts-noto-ui-core fonts-thai-tlwg
    chromium --version || chromium-browser --version
    pip install --upgrade pip
    pip install "setuptools>=65.0.0" "wheel>=0.38.0"
    pip cache purge
    pip install --no-cache-dir -r worker_report_requirements.txt
  startCommand: python src/report_worker.py
  envVars:
    - key: PYTHON_VERSION
      value: 3.11.9
